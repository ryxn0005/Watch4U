# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2

# HTTP / utils
httpx==0.27.2
python-multipart==0.0.12
python-dotenv==1.0.1

# Database
surrealdb==2.0.0

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0

# Component libs (placeholders — each owner pins what they actually need)
# Fall detection (Darrel): opencv-python, ultralytics, torch, ...
# Wi-Fi detection (Ryan): numpy, scipy, ...
# Triage (Jayce): pandas, ...
# RAG (Ryan): langchain, chromadb / qdrant-client, sentence-transformers, ...
